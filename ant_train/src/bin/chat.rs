/// ANT Chat — stub binary (full implementation in main.rs via inference module).
fn main() {
    println!("ANT Chat (Rust)");
    println!("Train first: cargo run --bin train");
    println!("Usage: cargo run --bin chat -- --ckpt checkpoints/rust/checkpoint_phaseC.safetensors");
}
